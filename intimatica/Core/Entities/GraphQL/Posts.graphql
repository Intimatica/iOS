query Posts {
  posts (sort:"published_at:desc", where:{tags: {id: []}}){
    id
    title
    image {
      url
    }
    tags {
      name
    }
    is_paid
      published_at
    post_type {
        __typename
    }
  }
}
