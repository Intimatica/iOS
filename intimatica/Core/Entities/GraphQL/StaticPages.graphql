query AboutPage($locale: String!) {
  aboutPage(locale: $locale) {
    content
  }
}

query HelpPage($locale: String!) {
  helpPage(locale: $locale) {
    content
  }
}

query TermsAndConditionsPage($locale: String!) {
  termsAndConditionsPage(locale: $locale) {
    content
  }
}

query PrivacyPolicyPage($locale: String!) {
  privacyPolicyPage(locale: $locale) {
    content
  }
}

query PremiumDescription($locale: String!) {
  premiumDescription(locale: $locale) {
    content
  }
}

query Terms($locale: String!) {
  term(locale: $locale) {
    content
    }
}

query Conditions($locale: String!) {
  condition(locale: $locale) {
    content
    }
}
