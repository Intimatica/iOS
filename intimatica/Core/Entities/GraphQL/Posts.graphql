query Posts($tagId: [Int!], $postTypeId: [Int!]) {
  posts (sort:"published_at:desc",  where:{tags: {id: [$tagId]}, post_type: {id:[$postTypeId]}}){
    id
    title
    image {
      url
    }
    tags {
      name
    }
    is_paid
      published_at
    post_type {
      name
    }
  }
}
